pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "Suncorp"

include(":data")
include(":domain")
include(":presentation")